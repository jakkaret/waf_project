# 🧠 WAF + ML/AI Integration Proposal
**แนวทางการใช้ Machine Learning (Isolation Forest) และ AI (Gemini Reasoning) สำหรับระบบ WAF + CDN**

เอกสารฉบับนี้จัดทำขึ้นเพื่อวิเคราะห์ความเป็นไปได้ในการนำข้อมูลจากตาราง `waf_logs` ไปใช้ควบคู่กับโมเดลตรวจจับความผิดปกติ (Isolation Forest) และการนำ Generative AI (Gemini) มาช่วยทำหน้าทียืนยันเหตุผลการบล็อก (Reasoning) รวมถึงเสนอแนะกฎความปลอดภัย (Rule Suggestion)

---

## 1. การนำ Log ไปเทรน Isolation Forest (Anomaly Detection)

**Isolation Forest** เป็นอัลกอริทึมแบบ Unsupervised Learning ที่ค้นหาจุดข้อมูลที่เบี่ยงเบนไปจากกลุ่มปกติ (Outliers) ได้ดีมาก โครงสร้างข้อมูลในตาราง `waf_logs` สามารถใช้เทรนโมเดลนี้ได้โดยต้องทำ **Feature Engineering** ดังนี้:

### 📊 Feature Engineering (การแปลงข้อมูลเข้าโมเดล)

เนื่องจากโมเดลต้องการข้อมูลที่เป็นตัวเลขเชิงปริมาณ เราต้องสกัดฟีเจอร์จาก Log ข้อความดังนี้:

| ฟีเจอร์ที่บันทึกใน Log | ฟีเจอร์ที่ต้องแปลงสำหรับ ML (Features) | ความสำคัญในการตรวจจับ |
| :--- | :--- | :--- |
| `url` & `query` | **URL Entropy (ความป้อนระเบียบ)** | ทราฟฟิกปกติจะมี Entropy ต่ำ ส่วนการยิง SQLi/XSS ที่เข้ารหัสอักขระพิเศษ (เช่น `%27`, `%20`) จะมี Entropy สูงมาก |
| `url` & `query` | **Special Character Count** | นับจำนวนตัวอักษรต้องสงสัย เช่น `'`, `"`, `;`, `--`, `<`, `>`, `*` |
| `request_length` | **Request Length (Bytes)** | ตรวจจับการพยายามโจมตีด้วย Payload ขนาดใหญ่ผิดปกติ (Buffer Overflow / POST injection) |
| `body_bytes_sent` | **Response Size (Bytes)** | ตรวจจับการเกิดข้อมูลรั่วไหล (Data Leakage) เช่น หน้าเว็บปกติตอบกลับ 10KB แต่รอบนี้ตอบกลับ 5MB |
| `request_time` | **Request Time (ms)** | ค้นหาจุดผิดปกติของการดึงข้อมูลล่าช้า (Time-based SQLi / ReDoS) |
| `ip` (ย้อนหลัง) | **Request Frequency (Rate)** | จำนวนครั้งที่ไอพีเดิมยิงเข้ามาในช่วง 1 และ 5 นาทีล่าสุด (ตรวจจับ DDoS/Bruteforce) |

### 🚨 ข้อจำกัดในการนำไปทำ Auto Block / Auto Suggest

1. **Auto Block (มีความเสี่ยงสูง):** Isolation Forest อาจมองพฤติกรรมแปลกๆ ที่ถูกกฎหมาย (เช่น แอดมินล็อกอินเข้ามาเปลี่ยนการตั้งค่าครั้งแรก หรือมีการเปิดฟีเจอร์อัปโหลดภาพขนาดใหญ่) เป็นความผิดปกติ (False Positives) หากใช้ Auto Block ทันทีอาจส่งผลให้ผู้ใช้งานจริงโดนบล็อกและแอปใช้งานไม่ได้
2. **Auto Suggest Rule (ทำไม่ได้โดยตรง):** โมเดลนี้ตอบได้เพียงแค่ตัวเลขคะแนนความเสี่ยง (Anomaly Score) และป้ายกำกับที่เป็น `-1` หรือ `1` เท่านั้น ไม่สามารถอธิบายเหตุผลหรือเขียนโค้ดเขียนกฎการบล็อก (SecRule/Regex) ให้เราได้

---

## 2. โครงสร้างระบบแจ้งเตือนแบบไฮบริด (Hybrid Real-time & Daily Summary)

เพื่อให้ระบบทำงานได้อย่างมีประสิทธิภาพสูงสุด ประหยัดทรัพยากร (API Token) และลดความรำคาญจากข้อความเตือนที่ถี่เกินไป (Alert Fatigue) เราจะจัดแบ่งระดับการทำงานออกเป็น 2 ชั้น (Two-Tier Strategy):

```
                       [ WAF Log Event ]
                               │
            ┌──────────────────┴──────────────────┐
            ▼                                     ▼
   [ Real-time Alert Filter ]            [ 24-Hour Scheduled Cron ]
   • เช็ค Heuristics โลคอล               • รวมสถิติทราฟฟิก & บล็อกสะสม
   • บล็อกถี่ยิบ หรือ ความเสี่ยงสูง          • ส่งข้อมูลสรุปให้ Gemini API
            │                                     │
            ▼                                     ▼
 [ Telegram: Alert ทันที ]               [ Telegram: Daily Security Summary ]
 (แสดงข้อมูลดิบ/ไอพี/ความถี่)               (รายงานวิเคราะห์เหตุผล & แนะนำ WAF Rules)
```

---

### 🟢 ส่วนที่ 1: Real-time Alert (แจ้งเตือนทันที - ไม่ใช้ AI)
เป็นการตรวจสอบด้วยเงื่อนไขความปลอดภัยและอัตราความถี่ (Local Heuristics) เพื่อความรวดเร็วและประหยัดค่าใช้จ่าย:
1. **ความรุนแรงสูง (High Severity):** หากตรวจพบ Log 403 ที่ถูกบล็อกจาก ModSecurity Rules ในกลุ่มวิกฤต เช่น SQL Injection รุนแรง, Remote Code Execution (RCE) หรือการพยายามทำ Path Traversal
2. **ความพยายามเจาะระบบซ้ำๆ (Frequent Blocks):** เช่น ไอพีเดิมถูกบล็อก 403 เกิน 5 ครั้งภายใน 1 นาที
* **ผลลัพธ์:** ส่งรายละเอียดการสกัดกั้น (IP, URL, Rule ID, Severity) เข้า Telegram ทันทีเพื่อความปลอดภัย โดยไม่ต้องเรียกใช้ Gemini ในระดับเรียลไทม์

---

### 🤖 ส่วนที่ 2: Daily Executive Security Summary (สรุปรายวันโดย Gemini)
ทุกๆ 24 ชั่วโมง (เช่น เวลาเที่ยงคืน) ระบบหลังบ้านจะรัน Cron Job/Worker เพื่อรวบรวมทราฟฟิกทั้งหมดของวันนั้น แล้วส่งให้ Gemini ทำการสรุปภาพรวมด้านความปลอดภัย (AI Security Report) เพื่อส่งแจ้งเตือนเข้า Telegram

#### 💬 ตัวอย่าง Prompt ที่ส่งให้ Gemini สำหรับรายงานรายวัน:
```text
คุณคือผู้เชี่ยวชาญด้าน Cyber Security (SOC Manager) 
กรุณาสรุปภาพรวมและวิเคราะห์ข้อมูลความปลอดภัยของระบบ WAF ประจำวันนี้ [ข้อมูลสะสม 24 ชม.]:

[สรุปข้อมูลประจำวัน]:
- จำนวนคำขอทั้งหมด: 54,200 ครั้ง
- จำนวนทราฟฟิกปกติผ่านได้: 53,820 ครั้ง
- จำนวนที่ WAF บล็อก (403/429): 380 ครั้ง
- ประเภทการโจมตีอันดับ 1: SQL Injection (195 ครั้ง) มาจาก IP 203.0.113.5
- ประเภทการโจมตีอันดับ 2: XSS (120 ครั้ง) มาจาก IP 198.51.100.12
- หน้าเว็บที่โดนยิงบ่อยที่สุด: /api/v1/auth/login และ /forum/posts

กรุณาเขียนรายงานสรุปประจำวันเป็นภาษาไทยสั้นๆ 3 ส่วน:
1. ภาพรวมสถานการณ์วันนี้ความปลอดภัยดีขึ้นหรือแย่อย่างไร? (Executive Summary)
2. พฤติกรรมที่น่าสงสัยและควรเฝ้าระวังเป็นพิเศษคืออะไร?
3. คำแนะนำในการเขียนกฎ WAF Rules หรือตั้งค่าเพิ่มเติมเพื่อความปลอดภัยสูงสุด
```

#### 📱 ตัวอย่างข้อความรายงานรายวันบน Telegram:
```text
📊 WAF DAILY EXECUTIVE SUMMARY
📅 ประจำวันที่: 2026-07-31
──────────────────────────────
📈 ทราฟฟิกรวม: 54,200 ครั้ง | บล็อกสำเร็จ: 380 ครั้ง (0.7%)

🤖 [AI REPORT BY GEMINI]
• ภาพรวมสถานการณ์: ระบบมีความปลอดภัยดี มีทราฟฟิกโจมตีเพิ่มขึ้นเล็กน้อย (0.7%) แต่ระบบ WAF สามารถสกัดกั้นการทำงานได้สำเร็จ 100%
• ข้อตรวจพบสำคัญ: พบพฤติกรรมผิดปกติในการสแกนช่องโหว่ SQL Injection แบบพยายามบายพาสหน้าล็อกอินอย่างเป็นระบบจาก IP 203.0.113.5
• คำแนะนำการป้องกัน:
  1. ควรเพิ่มระดับความเร็วในการจำกัดคำขอ (Rate Limiter) สำหรับพาร์ท /api/v1/auth/login เพื่อป้องกันความพยายาม Bruteforce
  2. แนะนำให้เพิ่ม IP 203.0.113.5 เข้าบัญชีดำถาวร (Permanent IP Blacklist)

Rule Suggestion:
SecRule REQUEST_URI "@contains /api/v1/auth/login" "id:100001,phase:1,deny,status:429..."
```

---

## 3. สรุปเปรียบเทียบข้อดีของแนวทางไฮบริด

* **ลดจำนวนการเรียกใช้ API:** จากเดิมต้องเรียก Gemini ทุกครั้งที่มี Log 403 (อาจเป็นร้อยหรือพันครั้งต่อวัน) ลดเหลือเพียง **1 ครั้งต่อวัน** เท่านั้น ช่วยควบคุมค่าใช้จ่ายได้อย่างมีประสิทธิภาพ
* **แก้ปัญหาแจ้งเตือนขยะ (Alert Fatigue):** แอดมินจะไม่รู้สึกรำคาญกับการแจ้งเตือนตลอดเวลา แต่ยังคงไม่พลาดเหตุการณ์ร้ายแรงจริงๆ (เพราะมี Real-time Heuristic บล็อกไอพีพฤติกรรมเสี่ยงสูงอยู่แล้ว)
* **ได้รับความรู้เชิงวิเคราะห์ (Insightful):** รายงานสรุปรายวันจะช่วยให้คุณวิเคราะห์แนวโน้มภัยคุกคามระยะยาวได้อย่างเป็นรูปธรรม เพื่อไปอัปเดตกฎความปลอดภัยหน้า Origin Server ได้อย่างเป็นระบบ
