server_tokens off;

map $http_upgrade $connection_upgrade {
    default upgrade;
    '' close;
}

upstream dvwa_backend {
    server dvwa:80;
}

upstream frp_tunnel_router {
    server 172.18.0.1:8085;
}

upstream cloudwaf_tunnel {
    server 172.18.0.1:8060;
    keepalive 32;
}

# 1. Default Fallback Server (Direct IP Access)
server {
    listen 8080 default_server;
    server_name _;

    access_log /var/log/nginx/access.json json_combined;
    error_log /var/log/nginx/error.log warn;
    include /etc/nginx/includes/captcha_server.conf;
    include /etc/nginx/includes/otp_server.conf;

    set $always_redirect off;
    modsecurity on;
    modsecurity_transaction_id "$request_id";

    location = /healthz {
        access_log off;
        return 200 "OK\n";
    }

    # Static Assets bypass rate limiting
    location ~* \.(?:css|js|jpg|jpeg|png|gif|ico|svg|webp|woff|woff2|ttf|json|map)$ {
        proxy_pass http://frp_tunnel_router;
        proxy_set_header Host $host;
        proxy_set_header X-Request-ID $request_id;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
    }

    location / {
        # Gen3 roadmap 3.1-A: lab-only telemetry mirror (fire-and-forget,
        # never affects the real response). Scope/redaction enforced
        # server-side in ml/capture_telemetry.py (allowlist, secret
        # redaction, size cap, retention) -- unchanged by this line.
        mirror /internal-ml-capture;
        mirror_request_body on;

        # Single auth_request per location is a real nginx constraint (not
        # multi-value). nginx's auth_request module only treats 401/403 as
        # valid "deny" statuses eligible for error_page mapping (other codes,
        # e.g. 418, make it log "unexpected status" and 500 instead) -- 403 is
        # ModSecurity's own block status here, so captcha and OTP share 401
        # via one combined check (nginx/includes/otp_server.conf) that tags
        # which gate failed on a header for the challenge page to read.
        auth_request /internal-shield-check;
        auth_request_set $retry_after $upstream_http_retry_after;
        auth_request_set $shield_type $upstream_http_x_shield_type;
        
        proxy_pass http://frp_tunnel_router;
        proxy_set_header Host $host;
        proxy_set_header X-Request-ID $request_id;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_set_header Connection "";
        proxy_redirect off;
        proxy_buffering off;
    }

    location = /internal-rate-limit {
        internal;
        proxy_method GET;
        proxy_pass http://host.docker.internal:8000/api/limiter/check;
        proxy_pass_request_body off;
        proxy_set_header Content-Length "";
        proxy_set_header X-Original-URI $request_uri;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header Host $host;
    }

    error_page 401 =429 @rate_limit_exceeded;
    location @rate_limit_exceeded {
        add_header Retry-After $retry_after always;
        add_header Content-Type text/plain always;
        return 429 "Too Many Requests. Rate limit exceeded. Please try again later.";
    }

    error_page 403 /403.html; 
    location = /403.html { root /usr/share/nginx/html; internal; }
    include includes/location_common.conf;
}

# 2. Juice Shop Target (via Self-Hosted FRP Private Tunnel)
server {
    listen 8080;
    server_name juice.waf-it-kku.online;

    access_log /var/log/nginx/access.json json_combined;
    error_log /var/log/nginx/error.log warn;
    include /etc/nginx/includes/captcha_server.conf;
    include /etc/nginx/includes/otp_server.conf;

    modsecurity on;
    modsecurity_transaction_id "$request_id";

    # Static Assets bypass rate limiting to prevent broken UI
    location ~* \.(?:css|js|jpg|jpeg|png|gif|ico|svg|webp|woff|woff2|ttf|json|map)$ {
        proxy_pass http://frp_tunnel_router;
        proxy_set_header Host $host;
        proxy_set_header Accept-Encoding "";
        proxy_set_header X-Request-ID $request_id;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
    }

    location / {
        # Gen3 roadmap 3.1-A: lab-only telemetry mirror (fire-and-forget,
        # never affects the real response). Scope/redaction enforced
        # server-side in ml/capture_telemetry.py (allowlist, secret
        # redaction, size cap, retention) -- unchanged by this line.
        mirror /internal-ml-capture;
        mirror_request_body on;

        # Single auth_request per location is a real nginx constraint (not
        # multi-value). nginx's auth_request module only treats 401/403 as
        # valid "deny" statuses eligible for error_page mapping (other codes,
        # e.g. 418, make it log "unexpected status" and 500 instead) -- 403 is
        # ModSecurity's own block status here, so captcha and OTP share 401
        # via one combined check (nginx/includes/otp_server.conf) that tags
        # which gate failed on a header for the challenge page to read.
        auth_request /internal-shield-check;
        auth_request_set $retry_after $upstream_http_retry_after;
        auth_request_set $shield_type $upstream_http_x_shield_type;

        proxy_pass http://frp_tunnel_router;
        proxy_set_header Host $host;
        proxy_set_header Accept-Encoding "";
        proxy_set_header X-Request-ID $request_id;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
    }

    location = /internal-rate-limit {
        internal;
        proxy_method GET;
        proxy_pass http://host.docker.internal:8000/api/limiter/check;
        proxy_pass_request_body off;
        proxy_set_header Content-Length "";
        proxy_set_header X-Original-URI $request_uri;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header Host $host;
    }

    error_page 401 =429 @rate_limit_exceeded;
    location @rate_limit_exceeded {
        add_header Retry-After $retry_after always;
        add_header Content-Type text/plain always;
        return 429 "Too Many Requests. Rate limit exceeded. Please try again later.";
    }

    error_page 403 /403.html;
    location = /403.html { root /usr/share/nginx/html; internal; }
}

# 3. DVWA Lab Target (via Self-Hosted FRP Private Tunnel)
server {
    listen 8080;
    server_name dvwa.waf-it-kku.online;

    access_log /var/log/nginx/access.json json_combined;
    error_log /var/log/nginx/error.log warn;
    include /etc/nginx/includes/captcha_server.conf;
    include /etc/nginx/includes/otp_server.conf;

    modsecurity on;
    modsecurity_transaction_id "$request_id";

    # Static Assets bypass rate limiting
    location ~* \.(?:css|js|jpg|jpeg|png|gif|ico|svg|webp|woff|woff2|ttf|json|map)$ {
        proxy_pass http://frp_tunnel_router;
        proxy_set_header Host $host;
        proxy_set_header Accept-Encoding "";
        proxy_set_header X-Request-ID $request_id;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
    }

    location / {
        # Gen3 roadmap 3.1-A: lab-only telemetry mirror (fire-and-forget,
        # never affects the real response). Scope/redaction enforced
        # server-side in ml/capture_telemetry.py (allowlist, secret
        # redaction, size cap, retention) -- unchanged by this line.
        mirror /internal-ml-capture;
        mirror_request_body on;

        # Single auth_request per location is a real nginx constraint (not
        # multi-value). nginx's auth_request module only treats 401/403 as
        # valid "deny" statuses eligible for error_page mapping (other codes,
        # e.g. 418, make it log "unexpected status" and 500 instead) -- 403 is
        # ModSecurity's own block status here, so captcha and OTP share 401
        # via one combined check (nginx/includes/otp_server.conf) that tags
        # which gate failed on a header for the challenge page to read.
        auth_request /internal-shield-check;
        auth_request_set $retry_after $upstream_http_retry_after;
        auth_request_set $shield_type $upstream_http_x_shield_type;

        proxy_pass http://frp_tunnel_router;
        proxy_set_header Host $host;
        proxy_set_header Accept-Encoding "";
        proxy_set_header X-Request-ID $request_id;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
    }

    location = /internal-rate-limit {
        internal;
        proxy_method GET;
        proxy_pass http://host.docker.internal:8000/api/limiter/check;
        proxy_pass_request_body off;
        proxy_set_header Content-Length "";
        proxy_set_header X-Original-URI $request_uri;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header Host $host;
    }

    error_page 401 =429 @rate_limit_exceeded;
    location @rate_limit_exceeded {
        add_header Retry-After $retry_after always;
        add_header Content-Type text/plain always;
        return 429 "Too Many Requests. Rate limit exceeded. Please try again later.";
    }

    error_page 403 /403.html;
    location = /403.html { root /usr/share/nginx/html; internal; }
}


# 4. VAmPI Lab Target (via CloudWAF self-hosted private tunnel)
#    Same chain as the FRP-backed hosts above: ModSecurity inspects the request
#    first, then it is handed to the tunnel server, which relays it down the
#    agent's outbound connection. The origin never accepts an inbound port.
server {
    listen 8080;
    server_name vampi.waf-it-kku.online;

    access_log /var/log/nginx/access.json json_combined;
    error_log /var/log/nginx/error.log warn;
    include /etc/nginx/includes/captcha_server.conf;
    include /etc/nginx/includes/otp_server.conf;

    modsecurity on;
    modsecurity_transaction_id "$request_id";

    error_page 403 /403.html;
    location = /403.html {
        root /usr/share/nginx/html;
        internal;
    }

    location ~* \.(?:css|js|jpg|jpeg|png|gif|ico|svg|webp|woff|woff2|ttf|json|map)$ {
        proxy_pass http://cloudwaf_tunnel;
        proxy_set_header Host $host;
        proxy_set_header Accept-Encoding "";
        proxy_set_header X-Request-ID $request_id;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
    }

    location / {
        # Gen3 roadmap 3.1-A: lab-only telemetry mirror (fire-and-forget,
        # never affects the real response). Scope/redaction enforced
        # server-side in ml/capture_telemetry.py (allowlist, secret
        # redaction, size cap, retention) -- unchanged by this line.
        mirror /internal-ml-capture;
        mirror_request_body on;

        proxy_pass http://cloudwaf_tunnel;
        proxy_set_header Host $host;
        proxy_set_header Accept-Encoding "";
        proxy_set_header X-Request-ID $request_id;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_read_timeout 60s;
    }
}

# 5. bWAPP Lab Target (via Self-Hosted FRP Private Tunnel)
server {
    listen 8080;
    server_name bwapp.waf-it-kku.online;

    access_log /var/log/nginx/access.json json_combined;
    error_log /var/log/nginx/error.log warn;
    include /etc/nginx/includes/captcha_server.conf;
    include /etc/nginx/includes/otp_server.conf;

    modsecurity on;
    modsecurity_transaction_id "$request_id";

    # Static Assets bypass rate limiting
    location ~* \.(?:css|js|jpg|jpeg|png|gif|ico|svg|webp|woff|woff2|ttf|json|map)$ {
        proxy_pass http://frp_tunnel_router;
        proxy_set_header Host $host;
        proxy_set_header Accept-Encoding "";
        proxy_set_header X-Request-ID $request_id;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
    }

    location / {
        # Gen3 roadmap 3.1-A: lab-only telemetry mirror (fire-and-forget,
        # never affects the real response). Scope/redaction enforced
        # server-side in ml/capture_telemetry.py (allowlist, secret
        # redaction, size cap, retention) -- unchanged by this line.
        mirror /internal-ml-capture;
        mirror_request_body on;

        # Single auth_request per location is a real nginx constraint (not
        # multi-value). nginx's auth_request module only treats 401/403 as
        # valid "deny" statuses eligible for error_page mapping (other codes,
        # e.g. 418, make it log "unexpected status" and 500 instead) -- 403 is
        # ModSecurity's own block status here, so captcha and OTP share 401
        # via one combined check (nginx/includes/otp_server.conf) that tags
        # which gate failed on a header for the challenge page to read.
        auth_request /internal-shield-check;
        auth_request_set $retry_after $upstream_http_retry_after;
        auth_request_set $shield_type $upstream_http_x_shield_type;

        proxy_pass http://frp_tunnel_router;
        proxy_set_header Host $host;
        proxy_set_header Accept-Encoding "";
        proxy_set_header X-Request-ID $request_id;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
    }

    location = /internal-rate-limit {
        internal;
        proxy_method GET;
        proxy_pass http://host.docker.internal:8000/api/limiter/check;
        proxy_pass_request_body off;
        proxy_set_header Content-Length "";
        proxy_set_header X-Original-URI $request_uri;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header Host $host;
    }

    error_page 401 =429 @rate_limit_exceeded;
    location @rate_limit_exceeded {
        add_header Retry-After $retry_after always;
        add_header Content-Type text/plain always;
        return 429 "Too Many Requests. Rate limit exceeded. Please try again later.";
    }

    error_page 403 /403.html;
    location = /403.html { root /usr/share/nginx/html; internal; }
}
